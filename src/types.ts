export type Project = {
      id: number;
      title: string;
      image: string;
      link?: string;
      github?: string;
}

export type Interest = {
      id: number;
      title: string;
      image: string;
      link?: string;
}

export type Skill = {
      id: number;
      title: string;
      image: string;
      link?: string;
}

export type Experience = {
      id: number;
      title: string;
      image: string;
      position?: string;
      date?: string;
      description?: string;
}

export type Education = {
      id: number;
      title: string;
      image: string;
      date?: string;
}

export type Contact = {
      id: number;
      title: string;
      image: string;
      link?: string;
}

export type Social = {
      id: number;
      title: string;
      image: string;
      link?: string;
}


